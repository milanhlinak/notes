import { NgModule } from '@angular/core';

const DIRECTIVES = [
];

@NgModule({
    imports: [],
    declarations: [...DIRECTIVES],
    providers: [],
    exports: [...DIRECTIVES]
})
export class DirectivesModule {
}
